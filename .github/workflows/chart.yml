name: Chart

# Chart version == git tag. Unlike the application charts, appVersion is NOT
# injected here: this chart packages a third-party application, so appVersion
# pins the upstream image and is maintained by hand in Chart.yaml.
on:
  push:
    tags: ["[0-9]+.[0-9]+.[0-9]+*"]
  workflow_dispatch:
    inputs:
      version:
        description: "Semver to publish (no leading v). Use only to re-publish."
        required: true
        type: string

permissions: {}

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    env:
      CHART_DIR: charts/nsi-envoy
    steps:
      - uses: actions/checkout@v4

      - name: Resolve version and registry
        id: cfg
        run: |
          set -euo pipefail
          v="${{ inputs.version || github.ref_name }}"
          v="${v#v}"
          if ! printf '%s' "$v" | grep -Eq '^[0-9]+\.[0-9]+\.[0-9]+'; then
            echo "::error::'$v' is not a semver version"; exit 1
          fi
          # OCI repository paths must be lowercase; the owner may not be.
          owner="$(printf '%s' '${{ github.repository_owner }}' | tr '[:upper:]' '[:lower:]')"
          echo "version=$v"                        >> "$GITHUB_OUTPUT"
          echo "oci=oci://ghcr.io/${owner}/charts" >> "$GITHUB_OUTPUT"

      - run: helm version

      - run: helm dependency list "$CHART_DIR"

      - run: helm lint "$CHART_DIR"

      - name: Package
        run: |
          set -euo pipefail
          helm package "$CHART_DIR" --destination dist \
            --version "${{ steps.cfg.outputs.version }}"
          ls -l dist

      - name: Log in to GHCR
        run: |
          printf '%s' '${{ secrets.GITHUB_TOKEN }}' \
            | helm registry login ghcr.io -u '${{ github.actor }}' --password-stdin

      - name: Push
        run: helm push dist/*.tgz "${{ steps.cfg.outputs.oci }}"

      - name: Log out of GHCR
        if: always()
        run: helm registry logout ghcr.io || true
